name: Format Carta Porte consumer

on:
  push:
    branches:
      - feat/resolver-backed-carta-porte-consumers

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/resolver-backed-carta-porte-consumers
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Format
        run: |
          python -m pip install black ruff
          black packages/python/catalogmx/catalogs/sat/carta_porte/material_peligroso.py
          ruff check --fix packages/python/catalogmx/catalogs/sat/carta_porte/material_peligroso.py
          black packages/python/catalogmx/catalogs/sat/carta_porte/material_peligroso.py
          git diff --check
      - name: Commit if needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name 'catalogmx-data[bot]'
          git config user.email 'catalogmx-data[bot]@users.noreply.github.com'
          git add packages/python/catalogmx/catalogs/sat/carta_porte/material_peligroso.py
          git commit -m 'style(data): format Carta Porte material consumer'
          git push origin HEAD:feat/resolver-backed-carta-porte-consumers
